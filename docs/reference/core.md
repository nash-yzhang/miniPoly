# core

The process and shared-memory primitives everything else is built on. Read
`buffer` before `minion`: the shared-memory namespace is the mechanism, and
`BaseMinion` is the API wrapped around it.

## miniPoly.core.buffer

::: miniPoly.core.buffer

## miniPoly.core.minion

::: miniPoly.core.minion

## miniPoly.core.contract

::: miniPoly.core.contract
